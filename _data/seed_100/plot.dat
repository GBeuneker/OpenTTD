set xlabel "X"
set ylabel "Y"
set autoscale

list = system('dir /b data_*.dat')
i=0

do for [file in list] {
    set title "Anomaly Detection ".i
    set term wxt i
    plot "data_".i.".dat"
    i=i+1
}

pause 1
reread